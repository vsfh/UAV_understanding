experiment: table3_convnext_gt_crop

runtime:
  device: cuda

data:
  root: ./um7
  ontology: ./configs/ontology.yaml
  labels: ./configs/core18_complete.txt
  bbox_annotations: ./um7/crop_bboxes_all_photos/crop_bboxes.json
  protocols: [session_disjoint]

model:
  family: encoder
  method: full_finetune
  repo_id: facebook/convnext-base-224
  revision: main
  path: ./hf_cache/convnext-base-224

representation:
  view: gt_crop

train:
  seeds: [43]
  epochs: 20
  batch_size: 4
  gradient_accumulation: 2
  learning_rate: 0.001
  backbone_learning_rate: 0.00001
  weight_decay: 0.01
  warmup_ratio: 0.1
  class_balance_power: 0.5
  num_workers: 12

test:
  batch_size: 4
  num_workers: 12

output:
  root: ./outputs/table3/convnext_gt_crop/{protocol}/seed{seed}
  checkpoint: ./outputs/table3/convnext_gt_crop/{protocol}/seed{seed}/best.pt
  results: ./results/table3/convnext_gt_crop/{protocol}/seed{seed}_test.json
  skip_existing: true
